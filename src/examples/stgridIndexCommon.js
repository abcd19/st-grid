
//общее для всех примеров
//cисок для комбобокса
export let list = [];
list.push({
  raw: String('Мой дядя самый'), display: String('Мой дядя самый честных правил когда в нешутку занемог'+i)
  });

  list.push({
    raw: String('Мой друг'), display: String('Мой друг')
    });

for(var i = 0; i < 20; i++)
{
  list.push({
  raw: String('raw'+i), display: String('display'+i)
  });
}

list.push({
  raw: String('Мой друг художник и поэт'), display: String('Мой друг художник и поэт')
  });








